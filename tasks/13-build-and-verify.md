# Task 14: Build & Verify

## Status: done

## Description
Verify the entire project compiles, passes type checks, and tests pass.

## Steps
1. Run `npm run lint` (tsc --noEmit) — no type errors
2. Run `npm test` — all tests pass
3. Run `npm run build` (tsup) — produces dist/index.js
4. Verify `npm run dev` runs without crashing

## Files
- None (verification only)
