# Task 8: Index Page Generator

## Status: pending

## Description
Generate a searchable index.html listing all reports.

## Steps
1. Create `src/writers/index-page.ts`
2. Implement `regenerateIndex(outputDir, state)` — reads all successful entries from state, sorts newest first, generates HTML table with search/filter JS
3. Implement local `escapeHtml()` utility

## Files
- `src/writers/index-page.ts`
